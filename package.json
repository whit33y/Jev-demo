{
  "name": "jev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/single.js",
  "scripts": {
    "single": "tsx watch --env-file=.env src/single.ts",
    "all": "tsx --env-file=.env src/example-questions.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
